<%= form_for(@era, url: era_path(@era), html: {method: 'patch', class: 'ui form'}) do |f| %>
<div class="ui raised segment">
<div class="ui accordion">
  <div class="active title">
    <i class="dropdown icon"></i>
    編集
  </div>
    <div class="active content">

    <div class="field">
      <label>時代名</label>
      <%= f.text_field :name, class: "" %>
    </div>

    <div class="field">
      <label>始まり</label>
      <%= f.text_field :start, class: "" %>
    </div>
    
    <div class="field">
      <label>終わり</label>
      <%= f.text_field :closed, class: "" %>
    </div>
    
    <div class="field">
      <label>終わり</label>
      <%= f.text_area :description, class: "" %>
    </div>
  
  <div class="actions">
    <%= f.submit "更新", class: 'ui button'%>
  </div>
  </div>
  </div>
</div>
<% end %>