
<div class="ui segment">
  <h3 class="ui right aligned header">
    <%= link_to "新規登録", new_card_path, { :class => "ui teal basic button" } %>
  </h3>
  <h3 class="ui center aligned header">
    <%= search_form_for @q,  html: {class: 'ui input'} do |f| %>
    
      <%= f.search_field :name_cont %>
    
      <%= f.submit %>
    <% end %>
  </h3>
</div>